{
  "name": "waldrive-video",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "record": "node scripts/record.mjs",
    "capture": "node scripts/capture.mjs",
    "render": "cd hf && npx hyperframes render . --quality high --fps 30 --resolution landscape-4k --crf 17 --output ../out/waldrive-demo.mp4"
  },
  "devDependencies": {
    "playwright": "1.49.1"
  }
}
